#include<iostream>
using namespace std;
vector<int>rearrange(vector<int>arr,int n){
    vector<int>temp(n);
   int first=0;
   int last=n-1;
for(int i=0;i<n;i++){
    bool ans=true;
    if(arr[first]>arr[last]){
       
    }
}

}